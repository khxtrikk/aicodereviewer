apiVersion: v1
kind: Service
metadata:
  name: aicodereviewer-service
  labels:
    app: aicodereviewer
spec:
  selector:
    app: aicodereviewer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000
  type: LoadBalancer