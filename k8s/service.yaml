apiVersion: v1
kind: Service
metadata:
  name: aicodereviewer-service
  labels:
    app: aicodereviewer
spec:
  selector:
    app: aicodereviewer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer